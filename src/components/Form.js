import React from "react";

const Form = () => {
    return (

        <form>
            <input type="text" id="todoName" name="todoName" />
            <button type="submit">Dodaj</button>
        </form>
    )
}

export default Form